{
  "content": "{{content | replace: site.data.strings.newline, '\n' | replace: site.data.strings.quote, site.data.strings.escapedQuote }}",
  "title": "{{page.title | replace: site.data.strings.newline, '\n' | replace: site.data.strings.quote, site.data.strings.escapedQuote }}",
  "by": "{{page.by | replace: site.data.strings.newline, '\n' | replace: site.data.strings.quote, site.data.strings.escapedQuote }}",
  "permalink": "{{page.url | replace: site.data.strings.newline, '\n' | replace: site.data.strings.quote, site.data.strings.escapedQuote }}",
  "date": "{{page.date}}",
  "description": "{{page.description | replace: site.data.strings.newline, '\n' | replace: site.data.strings.quote, site.data.strings.escapedQuote }}"
}
